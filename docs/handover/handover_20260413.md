# CantoFlow Session Handover — 2026-04-13 HKT

## Session Summary

Attempted to install the `student-v1` release on Johnson's MacBook Air (original dev machine, hostname `Johnson-Tam-MBA-35564`). Completed all installation steps but **not yet tested** — Johnson needed to stop and return to this later.

---

## Work Completed

### Student Install on MBA (Original Dev Machine)

**Pre-conditions found:**
- CantoFlow was running as a **debug build** from external drive `/Volumes/JT2TB/CantoFlow/app/.build/debug/cantoflow`
- `~/bin/cantoflow` was the old dev launcher (ran `swift build` every launch)
- LaunchAgent `com.cantoflow.launchagent.plist` was loaded
- `~/.cantoflow.env` existed with `QWEN_API_KEY` set (compatible with new version)
- No `~/CantoFlow/` directory (student root)
- Whisper model `ggml-large-v3-turbo.bin` (1.5GB) existed in dev repo on external drive

**Steps performed:**
1. Unloaded LaunchAgent via `launchctl unload`
2. Removed `/Applications/CantoFlow.app` (old dev-built bundle)
3. Removed old `~/bin/cantoflow` dev launcher
4. Pre-copied Whisper model from external drive → `~/CantoFlow/third_party/whisper.cpp/models/` (saved 1.5GB download)
5. Ran `curl -fsSL https://raw.githubusercontent.com/johnson-greate/Cantoflow/main/install-student.sh | bash`

**Install result:**
- ✅ `/Applications/CantoFlow.app` installed (student-v1 pre-built)
- ✅ `whisper-cli` downloaded and installed to `~/CantoFlow/third_party/whisper.cpp/build/bin/`
- ✅ Whisper model detected as existing (skipped download)
- ✅ `~/.cantoflow.env` preserved (existing API key kept)
- ✅ Ollama installed + `gemma4:e2b` pulled (actual size: 7.2GB, not 2.5GB as script states)
- ❌ `~/bin/cantoflow` launcher was NOT correctly updated (stdin consumed by inner `curl | sh` Ollama installer caused script truncation)
- ✅ Launcher **manually fixed** by Claude — now correctly points to `/Applications/CantoFlow.app`
- ✅ Old LaunchAgent plist **removed**

**Known install bug discovered:**
`curl | bash` + inner `curl | sh` (Ollama installer) = stdin conflict. The Ollama installer's `sh` consumed remaining script bytes, causing `syntax error near unexpected token 'else'` at end, and silently corrupted the launcher heredoc. Fix: manual launcher rewrite. Should fix `install-student.sh` to use `/dev/tty` for the Ollama `read` prompt, and write launcher to a temp file instead of heredoc-in-pipe.

---

## Current State

- **Main branch**: `485364d` (unchanged from last session)
- **MBA install**: All files in place, `cantoflow` command NOT YET TESTED
- **LaunchAgent**: Removed from MBA
- **`~/.cantoflow.env`**: Has `QWEN_API_KEY` (TextPolisher checks both `DASHSCOPE_API_KEY` and `QWEN_API_KEY`, so this works)

---

## Pending / Next Actions

### Immediate (resume next session on MBA)
1. **Run `cantoflow` and test** — grant Accessibility / Input Monitoring / Microphone permissions when prompted, test Fn push-to-talk
2. **Fix `install-student.sh` stdin bug** — launcher heredoc corrupted when inner `curl | sh` runs. Fix: write launcher to temp file or use different mechanism

### Previously Pending (still open)
3. **Test `install-student.sh` on a true clean Mac** — MBA is not clean, still need fresh machine test
4. **Settings UI English remnants** — tabs "General", "Vocabulary", "API Keys" still in English
5. **Gemma4:e2b is 7.2GB not 2.5GB** — update the script's size estimate

---

## Key Decisions Made

1. **No LaunchAgent on student install** — `install-student.sh` does not set up a LaunchAgent; removed the old one from MBA
2. **QWEN_API_KEY is compatible** — no need to migrate existing `.cantoflow.env`
3. **Ollama size discrepancy** — gemma4:e2b pulled 7.2GB despite script saying "約 2.5GB"; script needs update
